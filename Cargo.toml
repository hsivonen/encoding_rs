[package]
name = "encoding_rs"
version = "0.1.0"
authors = ["Henri Sivonen <hsivonen@hsivonen.fi>"]
license = "MIT/Apache-2.0"
readme = "README.md"
repository = "https://github.com/hsivonen/encoding-rs"
build = "build.rs"

[lib]
name = "encoding_rs"
# The below line confuses rustfmt. Uncomment to build a staticlib.
# crate-type = ["rlib", "staticlib"]

[build-dependencies]
rusty-cheddar = "0.3.1"

[profile.release]
lto = true
